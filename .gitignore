.env
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.DS_Store
.playwright-mcp/
.claude/settings.local.json
issues to fix.md
image/issuestofix/

# Local debugging screenshots / dev artifacts (not part of the app)
*.png

# Eval artifacts — regenerable with `python -m tests.eval.loop_eval capture`.
# The harness code is tracked; its captured inputs and generated answers are not.
tests/eval/fixtures/
tests/eval/runs/
